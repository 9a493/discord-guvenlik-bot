<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Discord Güvenlik Botu</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #fff;
        }
        
        nav {
            background: rgba(0, 0, 0, 0.2);
            padding: 20px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            backdrop-filter: blur(10px);
        }
        
        .logo {
            font-size: 1.5em;
            font-weight: bold;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .nav-links {
            display: flex;
            gap: 30px;
            align-items: center;
        }
        
        .nav-links a {
            color: white;
            text-decoration: none;
            transition: opacity 0.3s;
        }
        
        .nav-links a:hover { opacity: 0.8; }
        
        .login-btn {
            background: #5865f2;
            padding: 12px 30px;
            border-radius: 8px;
            font-weight: bold;
            transition: transform 0.2s;
        }
        
        .login-btn:hover { transform: scale(1.05); }
        
        .hero {
            text-align: center;
            padding: 100px 20px;
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .hero h1 {
            font-size: 4em;
            margin-bottom: 20px;
            text-shadow: 2px 2px 10px rgba(0,0,0,0.3);
        }
        
        .hero p {
            font-size: 1.5em;
            opacity: 0.9;
            margin-bottom: 40px;
        }
        
        .cta-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
        }
        
        .btn {
            padding: 15px 40px;
            border-radius: 10px;
            text-decoration: none;
            font-weight: bold;
            font-size: 1.1em;
            transition: transform 0.2s;
            display: inline-block;
        }
        
        .btn:hover { transform: scale(1.05); }
        
        .btn-primary {
            background: #5865f2;
            color: white;
        }
        
        .btn-secondary {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            backdrop-filter: blur(10px);
        }
        
        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            max-width: 1200px;
            margin: 80px auto;
            padding: 0 20px;
        }
        
        .stat-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 40px;
            border-radius: 20px;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .stat-number {
            font-size: 3em;
            font-weight: bold;
            color: #43b581;
            margin-bottom: 10px;
        }
        
        .stat-label {
            font-size: 1.2em;
            opacity: 0.8;
        }
        
        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            max-width: 1200px;
            margin: 80px auto;
            padding: 0 20px;
        }
        
        .feature {
            background: rgba(255, 255, 255, 0.05);
            padding: 30px;
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .feature h3 {
            font-size: 1.5em;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .feature p {
            opacity: 0.9;
            line-height: 1.6;
        }
        
        .user-info {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
        }
        
        footer {
            text-align: center;
            padding: 40px 20px;
            margin-top: 100px;
            background: rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(10px);
        }
    </style>
</head>
<body>
    <nav>
        <div class="logo">
            🔒 Discord Güvenlik Botu
        </div>
        <div class="nav-links">
            <a href="/">Ana Sayfa</a>
            <a href="/docs">Dokümantasyon</a>
            <a href="/support">Destek</a>
            <% if (user) { %>
                <div class="user-info">
                    <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar" class="user-avatar">
                    <span><%= user.username %></span>
                </div>
                <a href="/dashboard" class="login-btn">Dashboard</a>
            <% } else { %>
                <a href="/login" class="login-btn">Discord ile Giriş</a>
            <% } %>
        </div>
    </nav>

    <div class="hero">
        <h1>🔒 Discord Güvenlik Botu</h1>
        <p>Sunucunuzu otomatik olarak koruyan profesyonel güvenlik çözümü</p>
        
        <div class="cta-buttons">
            <a href="https://discord.com/api/oauth2/authorize?client_id=<%= process.env.CLIENT_ID %>&permissions=1099780063366&scope=bot%20applications.commands" class="btn btn-primary" target="_blank">
                ➕ Sunucuna Ekle
            </a>
            <% if (user) { %>
                <a href="/dashboard" class="btn btn-secondary">
                    📊 Dashboard
                </a>
            <% } else { %>
                <a href="/login" class="btn btn-secondary">
                    🔐 Giriş Yap
                </a>
            <% } %>
        </div>
    </div>

    <div class="stats">
        <div class="stat-card">
            <div class="stat-number"><%= stats.guilds %></div>
            <div class="stat-label">🖥️ Aktif Sunucu</div>
        </div>
        <div class="stat-card">
            <div class="stat-number"><%= Math.floor(stats.users / 1000) %>K+</div>
            <div class="stat-label">👥 Korunan Kullanıcı</div>
        </div>
        <div class="stat-card">
            <div class="stat-number"><%= stats.uptime %></div>
            <div class="stat-label">⏱️ Dakika Uptime</div>
        </div>
    </div>

    <div class="features">
        <div class="feature">
            <h3>📩 Spam Koruması</h3>
            <p>Mesaj spam'ini akıllı algoritmalarla tespit eder ve otomatik olarak engeller. Ayarlanabilir eşik değerleri ile tam kontrol.</p>
        </div>
        <div class="feature">
            <h3>🎤 Ses Koruması</h3>
            <p>Ses kanallarında susturma ve sağırlaştırma spam'ini tespit eder. Kötüye kullanımı anında önler.</p>
        </div>
        <div class="feature">
            <h3>⚙️ Özelleştirilebilir</h3>
            <p>Her sunucu için ayrı ayarlar. Web dashboard'dan kolayca yapılandırın, gerçek zamanlı değiştirin.</p>
        </div>
        <div class="feature">
            <h3>⚖️ Akıllı Ceza</h3>
            <p>Aşamalı cezalandırma: 1. ihlal = 1dk timeout, 2. ihlal = 1sa timeout, 3. ihlal = kick. Tamamen özelleştirilebilir!</p>
        </div>
        <div class="feature">
            <h3>📊 Detaylı Loglar</h3>
            <p>Tüm ihlaller kaydedilir. Web dashboard'dan 90 günlük geçmişi görüntüleyin, raporlar oluşturun.</p>
        </div>
        <div class="feature">
            <h3>🛡️ 7/24 Koruma</h3>
            <p>Bot kesintisiz çalışır. %99.9 uptime garantisi ile sunucunuz her zaman korunur.</p>
        </div>
    </div>

    <footer>
        <p>&copy; 2026 Discord Güvenlik Botu. Tüm hakları saklıdır.</p>
        <p style="margin-top: 10px; opacity: 0.7;">
            <a href="/privacy" style="color: white; margin: 0 10px;">Gizlilik</a> |
            <a href="/terms" style="color: white; margin: 0 10px;">Şartlar</a> |
            <a href="/support" style="color: white; margin: 0 10px;">Destek</a>
        </p>
    </footer>
</body>
</html>